{
  "dependencies": {
    "concurrently": "3.4.0",
    "cross-env": "5.0.0",
    "express": "4.15.3",
    "nodemon": "1.11.0"
  },
  "devDependencies": {
    "@types/express": "4.0.35",
    "@types/node": "7.0.21",
    "ts-loader": "2.1.0",
    "tslint": "5.2.0",
    "tslint-loader": "3.5.3",
    "typescript": "2.3.2"
  },
  "main": "index.js",
  "name": "@welovecoding/backend",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"tsc -w\" \"cross-env NODE_PATH=./dist nodemon dist/index.js\"",
    "build": "cross-env NODE_ENV=production && tsc",
    "start-prod": "cross-env NODE_PATH=./dist && node dist/index.js"
  },
  "version": "0.0.0"
}
